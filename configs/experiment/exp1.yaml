# Simple configuration for a single run
project: grape_vine_classification
command:
  - ${env}
  - ${interpreter}
  - ${program}

optim: "Adam"
lr: 0.001
momentum: 0.9
batch_size: 16
epochs: 20
patience: 21